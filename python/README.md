files in this folder are python scripts.

> [!NOTE]  
Files without the `general` suffix are the original versions designed for specific datasets or analysis settings.
Files with the `general` suffix are more general-purpose versions. They accept parameters as input and can be adapted to different datasets.

